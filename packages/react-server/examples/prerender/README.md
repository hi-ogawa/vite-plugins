# react-server prerender

```sh
pnpm i
pnpm dev

# local preview
pnpm build
pnpm preview

# deploy vercel (static + edge)
pnpm vc-build
pnpm vc-release

# deploy cloudflare pages
pnpm cf-build
pnpm cf-release
```
